(function (exports, require, module, __fielname, __dirname) {
    var concatArray = function(str, arry) {
        return arry.map(function(element) {
            return str + ' ' + element;
        });
    }
});